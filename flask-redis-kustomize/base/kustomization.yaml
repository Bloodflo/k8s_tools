apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flask.yml
  - flask-service.yml
  - redis.yml
  - redis-service.yml

labels:
  - pairs:
      app: devops-course-2025
    includeSelectors: true
