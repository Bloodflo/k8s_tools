apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    io.kompose.service: grafana
spec:
  type: LoadBalancer
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  externalIPs:
    - 10.0.2.15
  selector:
    io.kompose.service: grafana
